# Uncomment the next line to define a global platform for your project

target 'PurchasesHybridCommon' do
  platform :ios, '13.0'
  use_frameworks!

  pod 'RevenueCat', :git => 'https://github.com/RevenueCat/purchases-ios.git', :branch => 'break-vc-balances-out-of-customerInfo'

  target 'PurchasesHybridCommonTests' do
    # Pods for testing
    pod 'Quick', '5.0.1'
    pod 'Nimble', '10.0.0'
  end

  target 'PurchasesHybridCommonIntegrationTests' do
    # Pods for testing
    pod 'Quick', '5.0.1'
    pod 'Nimble', '10.0.0'
  end

end

target 'PurchasesHybridCommonUI' do
  platform :ios, '13.0'
  use_frameworks!

  pod 'RevenueCat', :git => 'https://github.com/RevenueCat/purchases-ios.git', :branch => 'break-vc-balances-out-of-customerInfo'
  pod 'RevenueCatUI', :git => 'https://github.com/RevenueCat/purchases-ios.git', :branch => 'break-vc-balances-out-of-customerInfo'
end

target 'ObjCAPITester' do
  platform :ios, '13.0'
  use_frameworks!

  pod 'RevenueCat', :git => 'https://github.com/RevenueCat/purchases-ios.git', :branch => 'break-vc-balances-out-of-customerInfo'
  pod 'RevenueCatUI', :git => 'https://github.com/RevenueCat/purchases-ios.git', :branch => 'break-vc-balances-out-of-customerInfo'
end
